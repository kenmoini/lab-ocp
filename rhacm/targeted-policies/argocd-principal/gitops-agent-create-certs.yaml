---
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: gitops-spoke-create-certs
  # namespace: argocd-principal
  annotations:
    policy.open-cluster-management.io/description: Creates and distributes the ArgoCD Agent credentials to Managed Clusters
    policy.open-cluster-management.io/categories: OCPOM - OpenShift Operator Management
    policy.open-cluster-management.io/standards: Kemo Labs 2026
    policy.open-cluster-management.io/controls: OLM-SUB - Operator Installation
spec:
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: gitops-spoke-auto-cert
        spec:
          remediationAction: enforce
          severity: critical
          object-templates-raw: |
            {{/* ## Loop through the ManagedClusters - check for the spoke label, then create the Certificates ## */}}
            {{- $managedClusterList := (lookup "cluster.open-cluster-management.io/v1" "ManagedCluster" "" "" "policy.kemo.dev/argocd-agent-auto").items }}
            {{- range $managedCluster := $managedClusterList }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: cert-manager.io/v1
                kind: Certificate
                metadata:
                  name: cluster-{{ $managedCluster.metadata.name }}
                  namespace: argocd-principal
                spec:
                  secretName: aac-{{ $managedCluster.metadata.name }}
                  commonName: {{ $managedCluster.metadata.name }}
                  isCA: false
                  duration: 8760h
                  privateKey:
                    algorithm: RSA
                    size: 4096
                  issuerRef:
                    name: argocd-agent-ca
                    kind: Issuer
            {{- end }}

    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: gitops-spoke-auto-sec
        spec:
          remediationAction: enforce
          severity: critical
          object-templates-raw: |
            {{/* ## Loop through the ManagedClusters - check for the spoke label, then create the Secret on the Hub ## */}}
            {{- $managedClusterList := (lookup "cluster.open-cluster-management.io/v1" "ManagedCluster" "" "" "policy.kemo.dev/argocd-agent-auto").items }}
            {{- with $managedClusterList }}
            {{- range $managedCluster := . }}
            {{- $mcn := $managedCluster.metadata.name }}
            {{- $secName := printf "aac-%s" $mcn }}
            {{- $certData := (fromSecret "argocd-principal" $secName "tls.crt") }}
            {{- $keyData := (fromSecret "argocd-principal" $secName "tls.key") }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                kind: Secret
                metadata:
                  name: cluster-{{ $mcn }}
                  namespace: argocd-principal
                  labels:
                    argocd-agent.argoproj-labs.io/agent-name: '{{ $mcn }}'
                    argocd.argoproj.io/secret-type: cluster
                  annotations:
                    argocd.argoproj.io/skip-reconcile: 'true'
                    managed-by: argocd-agent
                type: Opaque
                stringData:
                  server: '{{ printf "https://hub-argocd-agent-principal-resource-proxy:9090?agentName=%s" $mcn }}'
                  name: '{{ $mcn }}'
                  config: |
                    {
                      "tlsClientConfig": {
                        "insecure": false,
                        "caData": {{ (fromSecret "argocd-principal" "argocd-agent-ca" "tls.crt") | base64enc | quote }},
                        "certData": {{ $certData | base64enc | quote }},
                        "keyData": {{ $keyData | base64enc | quote }}
                      }
                    }
            {{- end }}
            {{- end }}

    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: gitops-spoke-managed-cert
        spec:
          remediationAction: enforce
          severity: critical
          object-templates-raw: |
            {{/* ## Loop through the ManagedClusters - check for the spoke label, then create the Certificates ## */}}
            {{- $managedClusterList := (lookup "cluster.open-cluster-management.io/v1" "ManagedCluster" "" "" "policy.kemo.dev/argocd-agent-managed").items }}
            {{- range $managedCluster := $managedClusterList }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: cert-manager.io/v1
                kind: Certificate
                metadata:
                  name: cluster-{{ $managedCluster.metadata.name }}
                  namespace: argocd-principal
                spec:
                  secretName: aac-{{ $managedCluster.metadata.name }}
                  commonName: {{ $managedCluster.metadata.name }}
                  isCA: false
                  duration: 8760h
                  privateKey:
                    algorithm: RSA
                    size: 4096
                  issuerRef:
                    name: argocd-agent-ca
                    kind: Issuer
            {{- end }}


    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: gitops-spoke-managed-sec
        spec:
          remediationAction: enforce
          severity: critical
          object-templates-raw: |
            {{/* ## Loop through the ManagedClusters - check for the spoke label, then create the Secret on the Hub ## */}}
            {{- $managedClusterList := (lookup "cluster.open-cluster-management.io/v1" "ManagedCluster" "" "" "policy.kemo.dev/argocd-agent-managed").items }}
            {{- with $managedClusterList }}
            {{- range $managedCluster := . }}
            {{- $mcn := $managedCluster.metadata.name }}
            {{- $secName := printf "aac-%s" $mcn }}
            {{- $certData := (fromSecret "argocd-principal" $secName "tls.crt") }}
            {{- $keyData := (fromSecret "argocd-principal" $secName "tls.key") }}
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                kind: Secret
                metadata:
                  name: cluster-{{ $mcn }}
                  namespace: argocd-principal
                  labels:
                    argocd-agent.argoproj-labs.io/agent-name: '{{ $mcn }}'
                    argocd.argoproj.io/secret-type: cluster
                  annotations:
                    argocd.argoproj.io/skip-reconcile: 'true'
                    managed-by: argocd-agent
                type: Opaque
                stringData:
                  server: '{{ printf "https://hub-argocd-agent-principal-resource-proxy:9090?agentName=%s" $mcn }}'
                  name: '{{ $mcn }}'
                  config: |
                    {
                      "tlsClientConfig": {
                        "insecure": false,
                        "caData": {{ (fromSecret "argocd-principal" "argocd-agent-ca" "tls.crt") | base64enc | quote }},
                        "certData": {{ $certData | base64enc | quote }},
                        "keyData": {{ $keyData | base64enc | quote }}
                      }
                    }
            {{- end }}
            {{- end }}