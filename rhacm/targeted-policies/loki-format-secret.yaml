---
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: rh-loki-format-obc-secret
  namespace: openshift-logging
  annotations:
    policy.open-cluster-management.io/description: Reconfigures the Loki OBC Secret into a new one to be used by Loki instances
    policy.open-cluster-management.io/categories: OCPOM - OpenShift Operator Management
    policy.open-cluster-management.io/standards: Kemo Labs 2025
    policy.open-cluster-management.io/controls: OLM-INST - Operator Instance
spec:
  disabled: false
  policy-templates:
    - extraDependencies:
        - apiVersion: policy.open-cluster-management.io/v1
          kind: ConfigurationPolicy
          name: rh-loki-obc
          namespace: ""
          compliance: Compliant
      objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: rh-loki-obc-secret
        spec:
          remediationAction: enforce
          severity: critical
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                kind: Secret
                metadata:
                  name: logging-loki-s3
                  namespace: openshift-logging
                type: Opaque
                data:
                  # Example using hub function to pull from a Secret in the hub cluster
                  #access_key_id: '{{hub fromSecret "openshift-logging" "loki" "AWS_ACCESS_KEY_ID" hub}}'
                  #access_key_secret: '{{hub fromSecret "openshift-logging" "loki" "AWS_SECRET_ACCESS_KEY" hub}}'
                  #bucketnames: '{{hub (lookup "objectbucket.io/v1alpha1" "ObjectBucketClaim" "openshift-logging" "loki").spec.bucketName | base64enc hub}}'
                  access_key_id: '{{ fromSecret "openshift-logging" "loki" "AWS_ACCESS_KEY_ID" }}'
                  access_key_secret: '{{ fromSecret "openshift-logging" "loki" "AWS_SECRET_ACCESS_KEY" }}'
                  bucketnames: '{{ (lookup "objectbucket.io/v1alpha1" "ObjectBucketClaim" "openshift-logging" "loki").spec.bucketName | base64enc }}'
                  endpoint: '{{ "https://s3.openshift-storage.svc" | base64enc }}'
                  region: '{{ "the-moon" | base64enc }}'
