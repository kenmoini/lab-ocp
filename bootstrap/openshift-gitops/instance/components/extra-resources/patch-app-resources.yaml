apiVersion: argoproj.io/v1beta1
kind: ArgoCD
metadata:
  name: openshift-gitops
spec:
  controller:
    resources:
      limits:
        cpu: '4'
        memory: 16Gi
      requests:
        cpu: 250m
        memory: 1Gi